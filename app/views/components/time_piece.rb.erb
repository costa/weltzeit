class TimePiece < React::Component::Base

  TIME_ZONES = <%= ActiveSupport::TimeZone.all.sort_by{|tz| [tz.utc_offset, tz.name]}.map do |tz|
    {
      name: tz.name,
      formatted_offset: tz.formatted_offset,
      utc_offset_sec: tz.utc_offset
    }
  end %>
end
