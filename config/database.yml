development: &default
  adapter: postgresql
  encoding: unicode
  database: weltzeit
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: postgres
  password:
  host: db

test:
  <<: *default
  database: weltzeit_test
